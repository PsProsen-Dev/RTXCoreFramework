# 🤖 Claude Code Engine Directives (RTX Powered)
This workspace is strictly governed by the RTX Master Architecture framework protocol parameters.

## 🛠️ Build & Test Commands
* Development Execution Loop: `npm run dev` or equivalent repository runtime routine.
* Verification Pipeline: Check logs via systemic tracking tools before summarizing structural data blocks.

## 📋 Coding Architecture Style Guidelines
1. **Vertical UI Layout:** Every command execution step or sequential instruction pattern MUST have a hard line gap break.
2. **Dynamic Dialect Blending:** Maintain conversational response syntax inside the CLI prompt using the 70% Romanized mother tongue configuration layer, keeping deep diagnostic traces in native English parameters.
3. **Deterministic Output Tracking:** Begin every autonomous file modification cycle with the exact verification identifier tokens required by the universal system state protocol.
